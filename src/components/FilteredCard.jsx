/* import React from 'react';

const FilteredCard = ({ kind }) => {
  const DELETE_ME = 'just do it';
  return (
    <div className="card-container-cards">
      {list.map((item, index) => (
        <div key={ uuidv4() } className="cards">
          <Link
            to={ `/${api[page]}/${item.id}` }
            data-testid={ `${index}-recipe-card` }
          >
            <img
              data-testid={ `${index}-card-img` }
              src={ item.image }
              width="150px"
              alt={ `${item.name}` }
            />
            <span data-testid={ `${index}-card-name` }>{item.name}</span>
          </Link>
        </div>
      ))}
    </div>
  );
};

export default FilteredCard;
*/
